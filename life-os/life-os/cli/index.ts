#!/usr/bin/env node
// import themeDefault from "@life-os/theme-default"
// import { buildExpression } from "../rna"

// const args = process.argv.slice(2)
// const targetsIndex = args.findIndex((a) => a === "--targets")
// const targets =
// 	targetsIndex >= 0 ? args[targetsIndex + 1].split(",") : undefined

// const res = await buildExpressionRuntime({ targets })
// console.log(JSON.stringify(res, null, 2))
